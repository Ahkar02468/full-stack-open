{
  "name": "blog-list",
  "version": "1.0.0",
  "description": "To list fav blog list",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf dist && cd ../part-2/part2-notes-frontend/ && npm run build && cp -r dist ../../part-4",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "test": "node --test"
  },
  "author": "AKSB",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "dotnet": "^1.1.4",
    "express": "^4.19.2",
    "mongoose": "^8.4.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.1"
  }
}
